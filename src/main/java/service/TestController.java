package service;

import io.javalin.http.Handler;
import model.User;

public class TestController {
    public static Handler getUser = ctx -> {
        //User user = new User(1, "Jonatan");
        //ctx.json(user);
    };
}
